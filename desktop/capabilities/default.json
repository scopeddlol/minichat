{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the local connect screen. Remote instance pages deliberately get no IPC access.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
