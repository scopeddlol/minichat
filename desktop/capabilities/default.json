{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the local connect screen, plus global shortcuts for voice hotkeys. Remote instance pages deliberately get no IPC access.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "global-shortcut:default"
  ]
}